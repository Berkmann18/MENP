extends layout

block content
    form(method='POST', style='float:none').col-sm-4.mx-auto
        legend= 'Profile Deletion - ' + user.username
        p For security purposes (to prevent anyone else from deleting your account).
        .form-group
            input.form-control(type='hidden', name='username', value=user.username)
            label(for='password') Password
            input.form-control(type='password', name='password', autofocus, required)
        button.btn.btn-danger(type='submit') confirm and delete this account
        a.btn.float-right.btn-secondary(href='/usr/' + user.id) go back